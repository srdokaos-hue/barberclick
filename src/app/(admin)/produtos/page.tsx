import type { Metadata } from "next"
export const metadata: Metadata = { title: "Estoque" }
// Componente interativo completo em src/components/admin/StockPanel.tsx
// (gerado a partir de /mnt/user-data/outputs/BarberClickStockPanel.jsx)
import StockPanel from "@/components/admin/StockPanel"
export default function Page() { return <StockPanel/> }
